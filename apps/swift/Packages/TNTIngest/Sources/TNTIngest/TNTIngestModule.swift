// Local Ingest: loopback HTTP endpoint that Worker Agents push Session
// Events into via the bundled `tnt` CLI. Permanent-client v0 home for
// `LocalIngestServer` on `127.0.0.1`. See docs/adr/0001 and docs/adr/0003.

public enum TNTIngestModule {}
