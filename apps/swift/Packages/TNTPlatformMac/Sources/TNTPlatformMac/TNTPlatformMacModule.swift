// macOS-only OS integrations: Accessibility (Capture Set), global hotkey
// (Voice Turn open/close), menu-bar host (Capture Chip + state lamp). All
// permanent-client — only the device can read these resources. See
// docs/adr/0003 (concrete, not protocol-gated).

public enum TNTPlatformMacModule {}
