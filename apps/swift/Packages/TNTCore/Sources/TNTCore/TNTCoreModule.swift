// Shared types and utilities for the Personal Master Agent.
//
// Future home for cross-package value types (e.g. SessionEvent, VoiceTurn
// identifiers, CaptureSet) and Foundation extensions used across the
// permanent-client side of the Future Server Boundary.

public enum TNTCoreModule {}
