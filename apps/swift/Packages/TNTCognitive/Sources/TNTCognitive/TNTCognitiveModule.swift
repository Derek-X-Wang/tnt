// Cognitive Engine: durable thinking layer (Rewrite, summarize, prioritize,
// reflect, extract preferences). Server-future side of the Future Server
// Boundary — v0 ships `protocol CognitiveEngine` + `LocalOpenAIEngine`; v1
// adds `RemoteEngine`. See docs/adr/0003.

public enum TNTCognitiveModule {}
